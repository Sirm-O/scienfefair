import { User, UserRole } from '../types';

// This file is cleared of mock data as the application is now connected to a live database.
// The empty array remains to prevent import errors in the mock scoring utility, which is now dormant.
export const MOCK_USERS: (User & { password?: string })[] = [];
